@model FrontRailwayCateringERP.Models.TrainViewModel

@{
    ViewData["Title"] = "Edit Train";
    ViewData["ActivePage"] = "Trains";
}

<div class="page-title">
    <h2>Edit Train</h2>
    <p>Update train details such as name, number, and coaches</p>
</div>

<div class="form-section">
    <form method="post" action="/Train/EditTrain/@Model.TrainId">
        @Html.AntiForgeryToken()

        <div class="mb-4">
            <label class="form-label"><i class="bi bi-train-front-fill"></i> Train Name</label>
            <div class="input-group">
                <span class="input-group-text" style="background:rgba(255,138,31,0.08);border:1.5px solid rgba(0,0,0,0.08);border-radius:12px 0 0 12px;border-right:none;">
                    <i class="bi bi-train-front" style="color:#FF8A1F;"></i>
                </span>
                <input name="TrainName" type="text" class="form-control" value="@Model.TrainName" required style="border-radius:0 12px 12px 0;" />
            </div>
        </div>

        <div class="mb-4">
            <label class="form-label"><i class="bi bi-hash"></i> Train Number</label>
            <div class="input-group">
                <span class="input-group-text" style="background:rgba(255,138,31,0.08);border:1.5px solid rgba(0,0,0,0.08);border-radius:12px 0 0 12px;border-right:none;">
                    <i class="bi bi-123" style="color:#FF8A1F;"></i>
                </span>
                <input name="TrainNumber" type="text" class="form-control" value="@Model.TrainNumber" required style="border-radius:0 12px 12px 0;" />
            </div>
        </div>

        <div class="mb-4">
            <label class="form-label"><i class="bi bi-layers-fill"></i> Total Coaches</label>
            <div class="input-group">
                <span class="input-group-text" style="background:rgba(255,138,31,0.08);border:1.5px solid rgba(0,0,0,0.08);border-radius:12px 0 0 12px;border-right:none;">
                    <i class="bi bi-grid-3x3" style="color:#FF8A1F;"></i>
                </span>
                <input name="TotalCoaches" type="number" class="form-control" value="@Model.TotalCoaches" required style="border-radius:0 12px 12px 0;" />
            </div>
        </div>

        <div class="form-actions">
            <button type="submit" class="btn-orange"><i class="bi bi-check-lg"></i> Update Train</button>
            <a href="/Train/GetTrain" class="btn-secondary-glass"><i class="bi bi-x-lg"></i> Cancel</a>
        </div>
    </form>
</div>
